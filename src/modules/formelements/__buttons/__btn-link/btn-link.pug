mixin btn-link( data )
  if data.hover
    button.buttons__icon-link.buttons__icon-link--hover(value=data.value name=data.name)= data.text
  else  
    button.buttons__icon-link(name=data.name)= data.text