mixin btn-white( data )
  if data.hover
    button.button.--white.--button-hover(value=data.value name=data.name)= data.text
  else  
    button.button.--white(value=data.value name=data.name)= data.text
