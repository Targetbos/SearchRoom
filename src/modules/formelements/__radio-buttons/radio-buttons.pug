mixin radio-buttons( data )
  .radio-buttons
    .radio-buttons__wrap 
      label.radio-buttons__label 
        if data.checked
          input(type="radio" name=data.name value=data.value checked=true)
        else
          input(type="radio" name=data.name value=data.value checked=false)
        .radio-buttons__icon
        span= data.text