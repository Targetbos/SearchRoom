mixin checkbox-item( data )
  .checkbox-item
    label.checkbox-item__label
      if data.checked
        input(type="checkbox" name= data.name value=data.value checked=true)
      else
        input(type="checkbox" name= data.name value=data.value checked=false)
      .checkbox-item__icon
      span.checkbox-item__text= data.text